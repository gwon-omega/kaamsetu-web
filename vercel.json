{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "vite",
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter @shram-sewa/web build",
  "outputDirectory": "apps/web/dist",
  "ignoreCommand": "bash -lc 'if [ -z \"$VERCEL_GIT_PREVIOUS_SHA\" ]; then exit 1; fi; git diff --quiet \"$VERCEL_GIT_PREVIOUS_SHA\" \"$VERCEL_GIT_COMMIT_SHA\" -- apps/web packages/shared packages/ui-tokens package.json pnpm-lock.yaml pnpm-workspace.yaml turbo.json tsconfig.base.json'"
}
